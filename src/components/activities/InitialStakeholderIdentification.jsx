import React from 'react'
import StakeholderWizard from './stakeholders/StakeholderWizard'

const InitialStakeholderIdentification = (props) => {
    return <StakeholderWizard {...props} />
}

export default InitialStakeholderIdentification
